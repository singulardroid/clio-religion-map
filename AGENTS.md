## Agent skills

### Issue tracker

Issues and PRDs for this repo live as markdown files in `.scratch/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Triage labels use default canonical strings (`needs-triage`, `ready-for-agent`, etc.). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout (one `CONTEXT.md` and `docs/adr/` at the repo root). See `docs/agents/domain.md`.